import React from 'react';
import Alert from '../../../containers/Posts/Alert';

const CommentAlert = ({ open, setClose }) => {
  return (
    <Alert open={open} setClose={setClose}>
      
    </Alert>
  );
}

export default CommentAlert;